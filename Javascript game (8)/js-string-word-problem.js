function firstAndLast(str) {
    lastCharacter = (str.slice(0,6))
}

console.log(lastCharacter + firstAndLast + lastCharacter);

lastCharacter('Miguel');
lastCharacter('Angel');
lastCharacter('Gaby');